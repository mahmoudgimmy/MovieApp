<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movieapp" >
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:screenOrientation="sensorLandscape"
        android:theme="@style/Theme.MovieApp" >
        <activity
            android:name=".ui.movies.activities.MoviesActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MovieApp.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.moviedetails.activities.MovieDetailActivity"
            android:label="@string/title_item_detail"
            android:parentActivityName=".ui.movies.activities.MoviesActivity"
            android:theme="@style/Theme.MovieApp" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.movieapp.ui.movies.activities.MoviesActivity" />
        </activity>
    </application>

</manifest>